# ─── 문서 처리 ([1][2] 담당 B) ───────────────────────────
pymupdf>=1.24            # 기본 로더 — 회전된 정비 주기표 정상 처리
pypdf>=5.0               # 로더 비교군
pdfplumber>=0.11         # 로더 비교군 + 표 검출

# ─── LangChain ─────────────────────────────────────────
langchain>=0.3
langchain-core>=0.3
langchain-community>=0.3
langchain-text-splitters>=0.3

# ─── 임베딩·벡터스토어 ([3][4][5] 담당 C) ──────────────────
langchain-huggingface>=0.1
sentence-transformers>=3.0
faiss-cpu>=1.8
langchain-chroma>=0.1
rank-bm25>=0.2           # 하이브리드 검색(선택)

# ─── LLM ([7] 담당 A) ──────────────────────────────────
langchain-openai>=0.2
langchain-anthropic>=0.3 # 선택
langchain-upstage>=0.4   # 선택
transformers>=4.44       # 로컬 HF 모델
torch>=2.4
accelerate>=0.34

# ─── 평가 ([8] 담당 A) ─────────────────────────────────
bert-score>=0.3.13
ragas>=0.2               # 선택 — context precision/recall
datasets>=3.0
evaluate>=0.4

# ─── 공통 ──────────────────────────────────────────────
python-dotenv>=1.0
pandas>=2.2
numpy>=1.26
tqdm>=4.66
